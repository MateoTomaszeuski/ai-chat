apiVersion: v1
kind: Service
metadata:
  name: mateo-ai-backend
  namespace: $NAMESPACE
  labels:
    app: aichat-backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: aichat-backend
