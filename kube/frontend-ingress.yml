apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mateo-ai-frontend-ingress
  namespace: $NAMESPACE
spec:
  ingressClassName: nginx
  rules:
  - host: mateo-ai-chat.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mateo-ai-frontend
            port:
              number: 80
